#!/bin/bash
# Pre-commit hook: GitPro enforcement blocker
#
# All git commits should go through the GitPro skill which:
# 1. Validates code quality (via Claude Code PreToolUse hook)
# 2. Uses --no-verify to bypass this hook (since validation already passed)
#
# This hook blocks direct git commits and directs users to GitPro.
#
# BYPASS OPTIONS (emergency only):
#   SKIP_GIT_GUARD=1 git commit ...  # Emergency bypass

# Check for emergency bypass
if [ "$SKIP_GIT_GUARD" = "1" ]; then
    echo "⚠️  SKIP_GIT_GUARD: Bypassing GitPro enforcement"
    exit 0
fi

# Block direct commits
echo ""
echo "🚫 DIRECT GIT COMMIT BLOCKED"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""
echo "Use the GitPro skill for all git operations:"
echo ""
echo "  • \"checkpoint\" - Quick timestamped commit"
echo "  • \"commit\"     - Full conventional commit with changelog"
echo "  • \"merge\"      - Merge to main with version bump"
echo ""
echo "GitPro ensures:"
echo "  ✓ Code quality validation"
echo "  ✓ Conventional commit format with emojis"
echo "  ✓ Automatic changelog updates"
echo ""
echo "Emergency bypass: SKIP_GIT_GUARD=1 git commit ..."
echo ""
exit 1
